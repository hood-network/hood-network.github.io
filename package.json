{
    "name": "hnfront",
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "prebuild": "bun authors.ts",
        "dev": "bun run prebuild && astro dev",
        "build": "bun run prebuild && astro build",
        "preview": "bun run prebuild && astro preview",
        "astro": "astro"
    },
    "dependencies": {
        "astro": "^5.7.13"
    },
    "devDependencies": {
        "@types/bun": "^1.2.13"
    }
}
